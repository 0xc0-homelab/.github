#!/usr/bin/env bash
# Runs tofu against org/ with the secrets decrypted into the environment only.
# Usage: scripts/tofu plan | scripts/tofu init | ...
set -euo pipefail
cd "$(dirname "$0")/.."
exec sops exec-env secrets/tofu.sops.yaml "tofu -chdir=org $(printf '%q ' "$@")"
